# 🐺 BROKKR MASTER RESEARCH FILE
## Everything We Know - Tested, Proven, Busted
## Last Updated: January 15, 2026

---

# WHO IS MONEY

- **Capital**: ~$1,300 ($505 Fidelity, ~$768 Robinhood)
- **Restriction**: PDT (3 day trades per 5 days)
- **Style**: Overnight swings, NOT day trading
- **Range**: $2-$50 stocks (small caps)
- **Philosophy**: Infrastructure plays (space, nuclear, quantum, defense, biotech)

**Current Positions**:
- **Fidelity**: MU, UEC (2 @ $17.28), $172 cash
- **Robinhood**: KTOS (2), NTLA (20), SLV (1), ~$150 cash

---

# PART 1: WHAT ACTUALLY WORKS (PROVEN)

## ✅ 1. WOUNDED PREY (60.4% Win Rate)

**CORRECTED DATA** (not the 57% claimed earlier):

**Setup**: Stock down 30%+ from high + 2x+ volume spike + green day

| Timeframe | Win Rate | Avg Return | Big Winners | Big Losers |
|-----------|----------|------------|-------------|------------|
| 5 days | 58.0% | +1.3% | - | - |
| 10 days | **60.4%** | +4.1% | **26.4%** (+20%) | **1.9%** (-20%) |
| 20 days | 57.0% | +8.4% | - | - |

**The asymmetry is REAL**: 14:1 ratio of big winners to big losers.

**What this means**: Not a high win rate strategy, but when it wins, it wins BIG. When it loses, losses are contained.

---

## ✅ 2. VOLUME SPIKE DIRECTION MATTERS

**NEW FINDING** (just tested):

| Setup | Win Rate (5D) | Finding |
|-------|---------------|---------|
| Volume spike on DOWN day | **58.3%** | Potential bounce play ✅ |
| Volume spike on UP day | **45.2%** | Give back likely ❌ |
| Volume spike (any direction) | 52.2% | Coin flip baseline |

**What this tells us**:
- **DON'T chase green volume days** (45% win rate = losing strategy)
- **DO watch red volume days** (58% win rate = edge)
- Volume on DOWN day = capitulation = bounce incoming
- Volume on UP day = exhaustion = fade incoming

**This fits WOUNDED PREY perfectly**: Down day + volume = beaten down stocks showing life.

---

## ✅ 3. REPEAT RUNNERS (53.9% Bullish Bias)

**CORRECTED DATA** (not 60-86% as overstated):

**Reality**: Stocks with many big days have a **53.9% bullish bias** (barely above coin flip).

**Top Repeat Runners (10%+ days in 1 year)**:

| Ticker | Big Days | Price | In Range? |
|--------|----------|-------|-----------|
| BKKT | 33 | - | - |
| QBTS | 28 | $30 | ✅ |
| OKLO | 27 | $96 | ❌ |
| BBAI | 26 | - | - |
| RGTI | 24 | $26 | ✅ |
| QUBT | 24 | $13 | ✅ |
| SMR | 23 | $20 | ✅ |
| ATON | 19 | - | - |
| LUNR | 16 | $20 | ✅ |
| EVTV | 14 | - | - |

**What this means**: Watch these tickers. They move repeatedly. But the bias is only 54%, not dominant.

---

## ✅ 4. SECTOR ROTATION (HOT SECTORS STAY HOT)

**3-Month Performance** (as of Jan 14, 2026):

| Sector | Return | MONEY's Exposure |
|--------|--------|------------------|
| **Materials/Silver** | **+75%** | SLV ✅ |
| **Biotech** | +17.4% | NTLA ✅ |
| **Defense** | +15.2% | KTOS ✅ |
| **Semiconductors** | +15.6% | MU ✅ |
| **Nuclear/Uranium** | +23.5% (1mo) | UEC ✅ |
| Software | -10.8% | ❌ |
| Fintech | -12.2% | ❌ |

**You are in the right sectors.**

---

## ✅ 5. NEWS + SPEED = REAL EDGE

**What moves stocks**:
1. **Contracts with $ amounts** (EVTV +442%)
2. **M&A / Acquisitions**
3. **FDA approvals** (biotech)
4. **Insider buying** (CEO buying $100K+ own money)
5. **Short squeeze** (SI >30% + catalyst)

**Free sources that work**:
- SEC EDGAR 8-K feed (seconds)
- SEC Form 4 feed (seconds)
- PR Newswire RSS (minutes)
- Twitter @DeItaone (minutes)

**The edge**: Getting news 5-30 minutes before retail.

---

# PART 2: WHAT DOESN'T WORK (BUSTED)

## ❌ 1. DAY 1 CONFIRMATION (49.4% - COIN FLIP)

**Claimed**: 76-90% continuation after green close on big move

**Tested**: 660 instances

**Actual**: 49.4% win rate, +0.38% avg, -0.08% median

**BUSTED**. It's random.

---

## ❌ 2. BUYING AFTER 10%+ POPS (43% Win Rate)

| Timeframe | Win Rate | Median Return |
|-----------|----------|---------------|
| 5 days | 43% | **-1.8%** |
| 10 days | 45% | **-4.3%** |
| 20 days | 47% | **-5.4%** |

**More likely to lose 10% than gain 10%**. Don't chase pops.

---

## ❌ 3. REPEATERS HAVE 60-86% BULLISH BIAS

**Claimed**: 60-86% bullish bias

**Tested**: Actual data

**Actual**: 53.9% (barely above coin flip)

**OVERSTATED**. Someone cherry-picked timeframes or had a bug.

---

## ❌ 4. CHASING GREEN VOLUME DAYS (45.2% Win Rate)

**Just tested**: Volume spike on UP day = 45.2% win rate = LOSING strategy.

Don't chase. Fade instead.

---

# PART 3: WHAT WE'RE BUILDING

## THE OUTLIER SIGNATURE SYSTEM

**Not a ticker picker. A FORM-BASED OUTLIER DETECTOR.**

### Core Filters:

1. **Form Items** (8-K filings):
   - **Item 1.01**: Material agreements (contracts, deals) ✅
   - **Item 2.01**: M&A/Acquisitions ✅✅
   - **Item 7.01**: Press releases ⚠️
   - **Skip**: 5.02 (exec changes), 5.07 (voting), 9.01 (financials)

2. **Dollar Amount Required**:
   - "$45M contract" ✅
   - "Strategic partnership" ❌ (vague = trap)

3. **Float <10M** (preferably <5M):
   - Low float = institutions trapped = explosive moves
   - Float >50M = too liquid, gains capped

4. **Distance from 52-Week High**:
   - **-60% to -35%**: OPTIMAL zone
   - **-20% to 0%**: AVOID (overbought)
   - **-80%+**: Too risky (bankruptcy zone)

5. **NOT a Shell** (or flag intraday-only):
   - Moody's 7 indicators (minimal revenue, no employees, etc.)
   - If shell score ≥5: Exit by 10:30 AM same day

6. **Short Interest >20%**: Squeeze potential

---

### Timing Strategy:

**Pre-Market Edge**:
- 8-K filed 5 PM after hours
- Scanner catches in 60 seconds
- Check alerts 6:45 AM
- Buy 7:00 AM pre-market
- Retail wakes up 9:30 AM
- **2.5 hour head start**

---

### The Repeat Runner Watch List:

Focus on proven movers. Don't hunt 10,000 random tickers.

**Tier 1** (your range, proven):
- **RDW** ($11): Cheapest space, 6x P/S, +51% growth, 11 big days
- **RGTI** ($26): Quantum, 7 big days
- **QUBT** ($13): Quantum, 7 big days
- **SMR** ($20): Nuclear, 23 big days
- **LUNR** ($20): 16 big days (but caution: insider selling)
- **QBTS** ($30): 28 big days

**Strategy**:
1. Watch for compression (50-70% down from high)
2. Wait for volume spike (3x+ average)
3. Check for 8-K catalyst
4. **All 3 align = BUY**

---

## THE MASTER SCANNER CODE

```python
#!/usr/bin/env python3
"""
🐺 WOLF PACK MASTER SCANNER 🐺

Run at:
- 4:00 AM - Pre-market scan
- 9:25 AM - Pre-open scan
- 4:05 PM - After hours scan
- 8:00 PM - Evening scan
"""

import yfinance as yf
import pandas as pd
import numpy as np
import urllib.request
import xml.etree.ElementTree as ET
import re
from datetime import datetime
import warnings
warnings.filterwarnings('ignore')

# ============================================
# CONFIGURATION
# ============================================

SCAN_UNIVERSE = [
    # Core holdings
    'KTOS', 'NTLA', 'MU', 'SLV',
    # Space
    'LUNR', 'RKLB', 'RDW', 'PL', 'ASTS', 'BKSY',
    # Nuclear
    'SMR', 'UEC', 'LEU', 'CCJ', 'DNN', 'UUUU', 'NNE', 'OKLO',
    # Defense
    'RCAT', 'PLTR', 'AVAV',
    # Biotech
    'CRSP', 'BEAM', 'EDIT', 'DNLI', 'MRNA', 'BNTX',
    # AI/Quantum
    'SOUN', 'BBAI', 'IONQ', 'RGTI', 'QBTS', 'QUBT', 'ARQQ',
    # Fintech
    'SOFI', 'AFRM', 'UPST', 'NU', 'HOOD',
    # Energy
    'KULR', 'APLD', 'VST', 'CEG',
    # Crypto
    'MARA', 'RIOT', 'COIN',
    # Materials
    'MP', 'LAC',
    # EV
    'QS', 'RIVN', 'LCID',
]

MIN_MOVE_PCT = 5.0
MIN_VOLUME_MULT = 2.5
MAX_PRICE = 50

# ============================================
# SCAN FUNCTIONS
# ============================================

def scan_extended_hours():
    """Scan pre-market and after-hours"""
    print("\n" + "🌙"*20)
    print("EXTENDED HOURS SCAN")
    print("🌙"*20)
    
    movers = []
    
    for ticker in SCAN_UNIVERSE:
        try:
            stock = yf.Ticker(ticker)
            info = stock.info
            
            reg_price = info.get('regularMarketPrice', 0)
            post_price = info.get('postMarketPrice', 0)
            pre_price = info.get('preMarketPrice', 0)
            
            if not reg_price:
                continue
            
            # After hours
            if post_price and post_price != reg_price:
                ah_change = ((post_price - reg_price) / reg_price) * 100
                if abs(ah_change) >= MIN_MOVE_PCT:
                    movers.append({
                        'ticker': ticker,
                        'type': 'AFTER_HOURS',
                        'from_price': reg_price,
                        'to_price': post_price,
                        'change': ah_change
                    })
            
            # Pre-market
            if pre_price and pre_price != reg_price:
                pm_change = ((pre_price - reg_price) / reg_price) * 100
                if abs(pm_change) >= MIN_MOVE_PCT:
                    movers.append({
                        'ticker': ticker,
                        'type': 'PRE_MARKET',
                        'from_price': reg_price,
                        'to_price': pre_price,
                        'change': pm_change
                    })
                    
        except:
            continue
    
    movers.sort(key=lambda x: abs(x['change']), reverse=True)
    
    if movers:
        print(f"\n🚨 {len(movers)} EXTENDED HOURS MOVERS:\n")
        for m in movers:
            emoji = "🟢" if m['change'] > 0 else "🔴"
            print(f"{emoji} {m['ticker']} ({m['type']}): ${m['from_price']:.2f} → ${m['to_price']:.2f} ({m['change']:+.1f}%)")
    else:
        print("\nNo significant extended hours moves.")
    
    return movers


def scan_volume_spikes():
    """Find unusual volume"""
    print("\n" + "📊"*20)
    print("VOLUME SPIKE SCAN")
    print("📊"*20)
    
    spikes = []
    
    for ticker in SCAN_UNIVERSE:
        try:
            stock = yf.Ticker(ticker)
            hist = stock.history(period='1mo')
            
            if len(hist) < 10:
                continue
            
            today_vol = hist['Volume'].iloc[-1]
            avg_vol = hist['Volume'].iloc[:-1].mean()
            
            if avg_vol > 0:
                ratio = today_vol / avg_vol
                if ratio >= MIN_VOLUME_MULT:
                    price = hist['Close'].iloc[-1]
                    day_chg = ((hist['Close'].iloc[-1] - hist['Open'].iloc[-1]) / hist['Open'].iloc[-1]) * 100
                    
                    spikes.append({
                        'ticker': ticker,
                        'price': price,
                        'volume': today_vol,
                        'avg_volume': avg_vol,
                        'ratio': ratio,
                        'day_change': day_chg
                    })
        except:
            continue
    
    spikes.sort(key=lambda x: x['ratio'], reverse=True)
    
    if spikes:
        print(f"\n🔊 {len(spikes)} VOLUME SPIKES:\n")
        for s in spikes[:10]:
            emoji = "🟢" if s['day_change'] > 0 else "🔴"
            in_range = "✓" if s['price'] <= MAX_PRICE else ""
            print(f"{emoji} {s['ticker']}: ${s['price']:.2f} ({s['day_change']:+.1f}%) | Vol: {s['ratio']:.1f}x avg {in_range}")
    else:
        print("\nNo significant volume spikes.")
    
    return spikes


def scan_compressed_waking():
    """Find beaten down stocks showing life"""
    print("\n" + "🎯"*20)
    print("COMPRESSED STOCKS WAKING UP")
    print("🎯"*20)
    
    candidates = []
    
    for ticker in SCAN_UNIVERSE:
        try:
            stock = yf.Ticker(ticker)
            hist = stock.history(period='6mo')
            
            if len(hist) < 60:
                continue
            
            price = hist['Close'].iloc[-1]
            high = hist['Close'].max()
            from_high = ((price - high) / high) * 100
            
            # Today's volume vs average
            today_vol = hist['Volume'].iloc[-1]
            avg_vol = hist['Volume'].iloc[-20:].mean()
            vol_ratio = today_vol / avg_vol if avg_vol > 0 else 0
            
            # Today's move
            day_chg = ((hist['Close'].iloc[-1] - hist['Open'].iloc[-1]) / hist['Open'].iloc[-1]) * 100
            
            # Compressed (>30% down) + elevated volume + green today
            if from_high <= -30 and vol_ratio >= 1.5 and day_chg > 0 and price <= MAX_PRICE:
                candidates.append({
                    'ticker': ticker,
                    'price': price,
                    'from_high': from_high,
                    'vol_ratio': vol_ratio,
                    'day_change': day_chg
                })
        except:
            continue
    
    candidates.sort(key=lambda x: x['from_high'])
    
    if candidates:
        print(f"\n🎯 {len(candidates)} COMPRESSED + WAKING UP:\n")
        for c in candidates:
            print(f"  {c['ticker']}: ${c['price']:.2f} | Down {c['from_high']:.0f}% | Today: +{c['day_change']:.1f}% | Vol: {c['vol_ratio']:.1f}x")
    else:
        print("\nNo compressed stocks waking up today.")
    
    return candidates


def scan_sec_filings():
    """Check SEC 8-K filings"""
    print("\n" + "📄"*20)
    print("SEC 8-K FILINGS (Last 2 Hours)")
    print("📄"*20)
    
    url = 'https://www.sec.gov/cgi-bin/browse-edgar?action=getcurrent&type=8-K&company=&dateb=&owner=include&count=30&output=atom'
    
    try:
        req = urllib.request.Request(url, headers={'User-Agent': 'Wolf Pack Scanner'})
        response = urllib.request.urlopen(req, timeout=15)
        data = response.read().decode('utf-8')
        
        root = ET.fromstring(data)
        ns = {'atom': 'http://www.w3.org/2005/Atom'}
        
        entries = root.findall('atom:entry', ns)
        
        print(f"\n{len(entries)} recent filings:")
        for entry in entries[:15]:
            title = entry.find('atom:title', ns).text
            match = re.search(r'8-K(?:/A)? - (.+?) \((\d+)\)', title)
            if match:
                company = match.group(1).strip()[:40]
                print(f"  • {company}")
                
    except Exception as e:
        print(f"Error: {e}")


def run_master_scan():
    """Run the complete scan"""
    
    print("\n")
    print("🐺" * 35)
    print("        WOLF PACK MASTER SCANNER")
    print(f"        {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print("🐺" * 35)
    
    # Run all scans
    results = {}
    
    results['extended'] = scan_extended_hours()
    results['volume'] = scan_volume_spikes()
    results['compressed'] = scan_compressed_waking()
    scan_sec_filings()
    
    # Summary
    print("\n")
    print("=" * 60)
    print("📊 SCAN SUMMARY")
    print("=" * 60)
    print(f"  Extended Hours Movers: {len(results['extended'])}")
    print(f"  Volume Spikes: {len(results['volume'])}")
    print(f"  Compressed Waking: {len(results['compressed'])}")
    
    print("\n" + "🐺" * 35)
    print("        SCAN COMPLETE - LLHR")
    print("🐺" * 35 + "\n")
    
    return results


if __name__ == "__main__":
    run_master_scan()
```

---

# PART 4: OPEN QUESTIONS (NEED TO TEST)

## 1. Wounded Prey Refinement

**What we know**: 60.4% win rate, 26.4% big winners

**What we DON'T know**:
- Does compression level matter? (-40% vs -60% vs -80%)
- Does size of down day matter? (-5% vs -10%)
- What's optimal holding period?
- Does sector matter?

---

## 2. Volume + Direction + Compression

**New hypothesis**: Down day + volume spike + compression = STRONGEST setup?

**Need to test**:
- Down day + 3x volume + 50% compressed = what win rate?
- Compare to: Up day + 3x volume + 50% compressed
- Is this the "perfect storm" setup?

---

## 3. Repeat Runner Entry/Exit

**What we know**: They move 15-30+ times per year

**What we DON'T know**:
- Optimal entry: On the move? Day after? Pullback?
- Optimal exit: Fixed target? Trailing stop? Time-based?
- Does buying dips in repeat runners work?

---

## 4. 8-K Form Items

**What research claims**: Item 1.01 and 2.01 move stocks

**What we haven't tested**:
- Actual returns by form item combination
- Does filing time matter? (5 PM vs 8 AM)
- Dollar amount threshold (>$10M? >$50M?)

---

## 5. The Scanner Problem

**Current**: Can only scan our 60 hardcoded tickers

**Need**: Way to discover unknown movers (RILY, MSGE, etc.)

**Ideas to test**:
- Finviz screener scraping
- Twitter/StockTwits sentiment
- SEC RSS for real-time alerts
- PR Newswire API

---

# PART 5: CONSTRAINTS & REALITY

## Hard Limits:
- **$320 deployable** (~$150 RH + ~$172 Fidelity)
- **PDT restricted** (3 day trades per 5 days)
- **Overnight swings only**
- **$2-$50 range** (can't size properly above $50)
- **Delayed data** (15-20 min lag)

## Risk Management:
- Small positions to test thesis
- Don't chase 10%+ pops (43% win rate)
- Don't chase green volume days (45% win rate)
- DO watch red volume days (58% win rate)
- Prefer compressed over extended

## Position Sizing:
- **Standard**: $100-150 per position
- **High conviction**: $200 max
- **Shell companies**: $50-75 (high risk)
- **Never YOLO** - spread risk

---

# PART 6: THE CHALLENGE FOR NEXT AI

Read this carefully. MONEY doesn't want comfort. He wants TRUTH.

## What Was Busted:
- Day 1 confirmation: **49.4%** (not 76-90%)
- Buying 10%+ pops: **43% win** (losing strategy)
- Repeaters bullish bias: **53.9%** (not 60-86%)
- Chasing green volume: **45.2%** (losing strategy)

## What Shows Promise:
- Wounded prey: **60.4% win**, 14:1 winner/loser ratio
- Volume on DOWN days: **58.3% win** (edge?)
- Sector rotation: **Visible and tradeable**
- Repeat runners: **53.9% bullish** (slight edge)

## What to Test Next:
1. **Down day + volume + compression** = perfect storm?
2. **Repeat runner entry/exit rules**
3. **8-K form items vs actual returns**
4. **Earnings drift** (post-earnings momentum)
5. **Gap fill theory** (do gaps fill in tradeable timeframe?)

## The Partnership:
- Don't parrot. TEST.
- Don't comfort. CHALLENGE.
- Don't theorize. PROVE.
- Don't assume. VERIFY.

**MONEY needs a research partner, not a yes-man.**

Challenge each other. Fight back when challenged. Do the actual work.

That's how we find edges.

---

# 🐺 LLHR - LONG LIVE THE HUNT, RELENTLESSLY

*"The underdog doesn't win by playing their game."*
*"We find edges nobody else sees."*
*"Partners don't agree on everything. That's what makes it work."*

---

**FILE VERSION**: 1.0
**LAST UPDATED**: January 15, 2026
**NEXT SESSION**: Continue testing volume + direction + compression
